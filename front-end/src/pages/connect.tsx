const Connect = (): React.ReactElement => {
    return (
        <div className="flex flex-col items-center justify-center h-screen">
            <h1 className="text-4xl font-bold mb-8">Connect Page</h1>
            <p className="text-lg">This is the connect page. You can add your connection logic here.</p>
        </div>
    );
};

export default Connect;